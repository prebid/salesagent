# PR 3 Phase C — Cleanup (delete legacy test.yml)

## Briefing
**Where we are.** Week 4 mid-week. Phase B stable ≥48h; `ci.yml` is authoritative. Calendar: closing PR 3.

**What this PR does.** Tiny follow-up: delete `.github/workflows/test.yml`. Update `docs/development/ci-pipeline.md`'s "current state" section to point at `ci.yml` (full rewrite of that doc happens in PR 4).

**Files.** Delete `.github/workflows/test.yml`. Edit `docs/development/ci-pipeline.md` (one section).
