# BDD Step Assertion Completeness Audit

Generated: 2026-03-11 23:23

## Summary

- **Steps scanned**: 158
- **Then steps analyzed**: 67
- **Passed triage**: 44
- **Flagged for deep inspection**: 23
- **Confirmed issues**: 23

## Issues by Severity

### MISSING (3)

#### `then_real_validation_error` (tests/bdd/steps/generic/then_error.py:250)

**Step text**: "the error should be a real validation error, not simulated"

**Claims**: The error should be a real validation error, not a simulated/sandbox error — meaning the function should distinguish between genuine validation errors and simulated ones

**Actually tests**: Only checks that an "error" key exists in the context dictionary — does not inspect the error's type, source, or any property that would distinguish real from simulated

**Recommendation**: The assertion should inspect the error object/dict stored in ctx["error"] for properties that distinguish real validation errors from simulated ones. Specifically: (1) check that the error type/class is an AdCPError subclass (e.g., ValidationError) rather than a simulated/sandbox error, (2) verify the error does NOT originate from a sandbox/simulation pathway — this could be checking that a "simulated" or "sandbox" flag is absent/False, or that the error source is the real validation layer (e.g., Pydantic validation, schema enforcement) rather than a test harness or sandbox adapter. The key semantic contract is: the error was raised by actual business logic validation, not manufactured by a test/simulation pathway.

#### `then_response_status` (tests/bdd/steps/generic/then_success.py:15)

**Step text**: "the response status should be "{status}""

**Claims**: the response object has a status field whose value equals the given status string

**Actually tests**: only that a response key exists in ctx when status is "completed"; unconditionally fails for any other status value — never reads an actual status field from the response

**Recommendation**: extract the response object from ctx, read its actual status field (e.g. response.status, response["status"], or whatever the domain model exposes), and assert that field equals the expected status parameter — for all status values, not just "completed"

#### `then_no_real_api_calls` (tests/bdd/steps/generic/then_success.py:64)

**Step text**: "no real ad platform API calls should have been made"

**Claims**: that no real ad platform API calls (GAM SOAP, Broadstreet REST, etc.) were made during the scenario — the system stayed on the sandbox/mock path

**Actually tests**: nothing — the body is `pass` with a comment rationalizing that the harness makes this inherently true

**Recommendation**: assert that the adapter resolved during the scenario was the MockAdapter (or equivalent sandbox adapter), not a real platform adapter. Concretely: inspect the test context for which adapter class was used (e.g. `ctx["adapter"]` or the registry's resolution log) and assert it is an instance of the mock/sandbox adapter. Alternatively, if the harness patches outbound HTTP, assert that no calls were recorded on the HTTP mock (e.g. `responses` or `httpx_mock` call count is zero for external domains). A `pass` body is never acceptable — even if the harness makes the condition structurally true, the step must assert that structural truth so it fails loudly if the harness breaks.

### WEAK (14)

#### `then_error_invalid_params` (tests/bdd/steps/generic/then_error.py:92)

**Step text**: "the error message should indicate which parameters are invalid"

**Claims**: The error message should contain information about which specific parameters are invalid — i.e., the message text should reference parameter names or indicate invalidity.

**Actually tests**: Only that an error exists and has a non-empty message string. No check that the message references any parameter names, contains words like "invalid"/"parameter", or in any way indicates which parameters are problematic.

**Recommendation**: Assert that the error message contains at least one of the parameter names that were passed as invalid in the scenario (these should be stored in ctx by the When step, e.g. ctx["invalid_params"]), OR at minimum assert the message contains keywords like "invalid" or "parameter" to confirm it is describing parameter-level validation failures rather than a generic error. The strongest check would be: for each invalid parameter name provided in the scenario, assert that name appears in the error message string.

#### `then_error_format_id_structure` (tests/bdd/steps/generic/then_error.py:130)

**Step text**: "the error message should indicate FormatId must include agent_url and id"

**Claims**: The error message should indicate that FormatId must include both `agent_url` and `id` fields

**Actually tests**: That the error message contains either `agent_url` OR `formatid` (case-insensitive) — only one needs to match, and `formatid` is a generic fallback that doesn't verify the structural requirement about both fields being needed

**Recommendation**: Assert that the error message contains references to BOTH `agent_url` AND `id` as required fields of FormatId. The assertion should use AND logic (both must appear in the message) rather than OR logic (either suffices). The `formatid` fallback should be removed as it doesn't verify the structural requirement. The correct check is: the error message mentions `agent_url` AND mentions `id` (as a required field reference, not just any substring).

#### `then_error_has_fix_suggestion` (tests/bdd/steps/generic/then_error.py:152)

**Step text**: "the error should include a suggestion for how to fix the issue"

**Claims**: The error's suggestion field contains content that describes how to fix the issue — i.e., actionable remediation guidance, not just any non-empty string.

**Actually tests**: The error dict has a "suggestion" key whose value is truthy (non-empty). A suggestion containing "banana" or "42" would pass.

**Recommendation**: Since this is a generic step in a shared step library, it cannot know the scenario-specific fix text. Two correct paths: (1) Downgrade the step text to "the error should include a suggestion" (dropping the "how to fix" content claim) and keep the structural check, which is honest about what it verifies. (2) Make the step parameterized — "the error should include a suggestion containing {expected_fragment}" — so each scenario supplies the expected fix content and the assertion does a substring match. Path 1 is simpler and appropriate if other scenarios already validate suggestion content via more specific steps; path 2 is correct if this is the only step that checks suggestions. Either way, the current combination of a content-specific claim with a purely structural check is a semantic gap that weakens the test's value as a behavioral specification.

#### `then_suggestion_valid_values` (tests/bdd/steps/generic/then_error.py:173)

**Step text**: "the suggestion should provide valid parameter values"

**Claims**: The suggestion field in the error response should contain actual valid parameter values that the caller could use to correct their request

**Actually tests**: Only that the suggestion field exists and is non-empty — any truthy string like "please retry" would pass

**Recommendation**: Assert that the suggestion string contains concrete valid parameter values — e.g., verify it includes recognizable enum members, format identifiers, or a list of acceptable values. At minimum, check the suggestion matches a pattern indicating specific values are present (such as containing commas or quoted strings), rather than accepting any non-empty string as "providing valid parameter values"

#### `then_suggestion_agent_url_id` (tests/bdd/steps/generic/then_error.py:216)

**Step text**: "the suggestion should advise including agent_url (URI) and id fields"

**Claims**: The suggestion text should advise including both "agent_url" (URI) and "id" fields

**Actually tests**: Only checks that the suggestion contains "agent_url" or "uri" — the "id" field requirement is completely unchecked

**Recommendation**: Assert that the suggestion text contains references to both required fields: (1) "agent_url" or "uri", AND (2) "id". Change the `or` to two separate assertions — one for agent_url/uri presence and one for "id" presence — so both claimed requirements are verified.

#### `then_has_referrals` (tests/bdd/steps/generic/then_payload.py:75)

**Step text**: "the response should include creative_agents referrals"

**Claims**: the response contains creative_agents referrals — i.e., the creative_agents field is populated with referral entries

**Actually tests**: the creative_agents list is non-empty (len > 0), but does not verify that entries are well-formed referral objects with expected attributes

**Recommendation**: beyond non-emptiness, assert that each item in creative_agents has the structural attributes expected of a referral (e.g., agent name/type, capabilities, or whatever fields define a creative agent referral in the schema). This distinguishes "list contains real referrals" from "list contains arbitrary non-empty entries." At minimum, verify the first item is an instance of the expected referral type or has a required identifying field.

#### `then_format_assets` (tests/bdd/steps/generic/then_payload.py:110)

**Step text**: "each format should include asset requirements with type and dimensions"

**Claims**: Each format's assets should have both "type" and "dimensions" fields present

**Actually tests**: Only checks that each asset has a "type" field; completely ignores "dimensions"

**Recommendation**: Assert that every asset dict contains both "type" AND "dimensions" keys, since the step text explicitly promises validation of both fields

#### `then_type_handling` (tests/bdd/steps/generic/then_payload.py:265)

**Step text**: "the type handling should be {expected}"

**Claims**: the function should verify that "type" field handling (type filter parameter on creative format requests) behaves correctly — valid enum types are accepted, invalid types are rejected

**Actually tests**: checks only whether ctx contains "response" (valid) or "error" (invalid) — a generic success/failure binary check with zero verification that the response was actually filtered by type or that the error relates to type validation

**Recommendation**: For "valid" outcomes, the assertion should verify that the returned creative formats are actually filtered to match the requested type (e.g., if type="display" was requested, all returned formats have type=="display"). For "invalid" outcomes, the assertion should verify the error is specifically a type-validation error (not an unrelated crash). The current assertion only checks "did something succeed or fail" — it cannot distinguish a correct type filter from a broken endpoint that happens to return 200, nor a proper validation rejection from an unrelated exception. However, given this is a boundary test (valid/invalid partition testing), the coarse-grained pass/fail check is architecturally intentional — the When step already encodes the field-specific logic. The weakness is that "valid" doesn't confirm the type filter was actually applied, only that no exception was thrown.

#### `then_asset_types_handling` (tests/bdd/steps/generic/then_payload.py:275)

**Step text**: "the asset_types handling should be {expected}"

**Claims**: the asset_types handling should be valid/invalid — i.e., the production code correctly handles the specific asset_types boundary point (accepts valid asset_types, rejects invalid ones)

**Actually tests**: checks whether ctx contains "response" (valid) or "error" (invalid) — a generic success/failure check with no verification that the asset_types filter was actually applied or that results are correctly filtered by asset type

**Recommendation**: For "valid" outcomes, the assertion should verify that the returned creative formats actually respect the asset_types filter — e.g., when filtering by asset_types=["image"], every returned format should have an asset type matching "image"; when using OR semantics with ["image","video"], results should only contain formats with those asset types; when omitted, results should include all asset types. For "invalid" outcomes (unknown enum value, removed enum value), verifying that an error was raised is correct. The current check is correct for the invalid/error path but too weak for the valid path — it only confirms the call didn't crash, not that asset_types filtering actually worked. However, this is a BOUNDARY test (tagged @boundary), and the architectural intent is explicitly partition testing: "does the production code accept or reject this input?" This is documented in the helper's docstring. The semantic purpose is input validation boundary testing, not functional filter verification (which belongs in separate @filter scenarios). Given this is a boundary/partition test, checking accept/reject is the designed level of assertion — but the step text "handling should be valid" is ambiguous enough that it reads like it should verify correct handling behavior, not just acceptance. The weakness is in the step text naming, not the assertion logic for the boundary-test context.

#### `then_responsive_handling` (tests/bdd/steps/generic/then_payload.py:285)

**Step text**: "the responsive handling should be {expected}"

**Claims**: the responsive handling should be valid/invalid — verifies that the is_responsive filter boundary behavior (true/false/omitted) produces correct responsive-specific filtering outcomes

**Actually tests**: checks only whether ctx contains "response" (valid) or "error" (invalid) — a generic success/failure check with no inspection of responsive filtering behavior

**Recommendation**: For boundary tests, "valid" should verify not just that the call succeeded, but that the response respects the is_responsive boundary constraint. Specifically: when is_responsive=true, verify the returned formats all have responsive render dimensions (renders.dimensions.responsive with width or height); when is_responsive=false, verify none do; when omitted, verify both responsive and non-responsive formats are present. The current generic success check would pass even if the responsive filter were completely broken (returning all formats regardless of the flag), as long as no exception was raised.

#### `then_name_search_handling` (tests/bdd/steps/generic/then_payload.py:290)

**Step text**: "the name search handling should be {expected}"

**Claims**: the name search handling should be {expected} — function should verify that the name_search filter was handled correctly for the given boundary point (exact match, partial, case-insensitive, omitted, no-match)

**Actually tests**: only checks whether ctx has "response" (valid) or "error" (invalid) — a pure success/failure binary check with no verification of name_search semantics

**Recommendation**: For boundary tests where expected=valid, verifying that the call didn't error is a reasonable minimum for partition/boundary testing — the purpose is to confirm the production code accepts the input without crashing. However, the step should additionally verify name_search-specific semantics: for "exact name match" and "partial substring match", assert the response contains only formats whose names match the search term; for "case-insensitive match", assert matches are returned regardless of casing; for "no match (empty result)", assert the response is successful but the formats list is empty; for "omitted", assert all formats are returned unfiltered. The current assertion is a liveness check ("did it crash?") but does not validate that name_search filtering actually worked correctly — a broken implementation that ignores the name_search parameter entirely would still pass all these tests.

#### `then_disclosure_handling` (tests/bdd/steps/generic/then_payload.py:300)

**Step text**: "the disclosure handling should be {expected}"

**Claims**: the function should verify that the disclosure handling (filtering by disclosure positions) produced the expected valid/invalid outcome specifically for the disclosure field

**Actually tests**: the function checks whether ctx contains a "response" key (valid) or an "error" key (invalid) — a generic success/failure check with no awareness of which field (disclosure, wcag, dimension, etc.) was being tested

**Recommendation**: For boundary partition tests, the generic valid/invalid assertion is architecturally defensible IF each scenario's Given+When steps set up a context where only the disclosure field varies and the production code is actually invoked with that specific filter. The weakness is that the assertion cannot distinguish "disclosure validation rejected this" from "some other validation rejected this first." The correct assertion for "invalid" cases should verify that the error message or error code references the disclosure/disclosure_positions field specifically (e.g., check that ctx["error"] mentions "disclosure" or "supported_disclosure_positions"). For "valid" cases, the assertion should verify that the response was filtered according to the disclosure criteria (e.g., returned formats actually match the requested disclosure positions). Without field-specific checks, a misconfigured Given step could cause a different field to fail validation, and the test would still pass — silently testing the wrong boundary.

#### `then_input_ids_handling` (tests/bdd/steps/generic/then_payload.py:310)

**Step text**: "the input_format_ids handling should be {expected}"

**Claims**: the function should verify how input_format_ids specifically were handled (partitioned, filtered, or validated), as distinct from output_format_ids handling

**Actually tests**: calls the same generic _assert_partition_outcome helper that the output_format_ids step also calls — there is no input-specific verification; both steps produce identical runtime behavior

**Recommendation**: the assertion should inspect the partition/handling result specifically for the input_format_ids field — e.g., verify that ctx contains an input-specific outcome key (like "input_format_ids" partition result) and that the expected disposition (accepted, rejected, filtered) applies to the input IDs specifically, not to a shared/ambiguous partition outcome that conflates input and output handling

#### `then_no_sandbox_field` (tests/bdd/steps/generic/then_success.py:53)

**Step text**: "the response should not include a sandbox field"

**Claims**: the serialized response should not contain a sandbox field at all — the key should be entirely absent from the response payload

**Actually tests**: checks that getattr(resp, 'sandbox', None) returns None, which passes both when the attribute is truly absent AND when the attribute exists with a None value — it cannot distinguish between "field not included" and "field included as null"

**Recommendation**: verify the field is absent from the serialized representation (model_dump output) rather than checking the attribute value is None — for a Pydantic response model, 'sandbox' should not appear as a key in model_dump(), because a field present with value None still serializes as {"sandbox": null} which means the response DOES include a sandbox field

### COSMETIC (6)

#### `then_type_filtering_result` (tests/bdd/steps/generic/then_payload.py:202)

**Step text**: "the type filtering should result in {expected}"

**Claims**: the type filtering operation should produce a specific result ("valid" or "invalid")

**Actually tests**: checks whether the production call succeeded (response exists in ctx) or failed (error exists in ctx), matching against the expected outcome string

**Recommendation**: The helper `_assert_partition_outcome` correctly validates the outcome of the filtering operation — "valid" asserts a response was returned without error, "invalid" asserts an error was raised. The step text "should result in {expected}" where expected is "valid"/"invalid" is a boundary/partition test pattern: it verifies that the production code accepts or rejects the input. The assertion semantically matches the claim. No change needed.

#### `then_format_ids_filtering_result` (tests/bdd/steps/generic/then_payload.py:207)

**Step text**: "the format_ids filtering should result in {expected}"

**Claims**: that the format_ids filtering operation produced the expected outcome (valid or invalid) — i.e., that specific format_id filter values are accepted or rejected by the production code

**Actually tests**: checks whether ctx contains a "response" key (for valid) or an "error" key (for invalid) — confirming the operation succeeded or failed as expected

**Recommendation**: The assertion is functionally correct for what these scenarios test. These are partition/boundary tests where each scenario row provides a specific filter input (e.g., "all_ids_match", "partial_match", "no_match") and asserts that the production code either accepts it (returns a response) or rejects it (raises an error). The helper `_assert_partition_outcome` correctly verifies this success/failure dichotomy. The step text "filtering should result in valid/invalid" accurately describes checking whether the filtering operation succeeds or fails. No change needed — the shared helper is the correct pattern for a family of partition steps that all verify the same accept/reject boundary.

#### `then_format_ids_handling` (tests/bdd/steps/generic/then_payload.py:270)

**Step text**: "the format_ids handling should be {expected}"

**Claims**: the format_ids handling should be valid/invalid — verifies that the format_ids filter parameter was correctly handled by production code (accepted valid inputs, rejected invalid ones)

**Actually tests**: checks whether ctx contains "response" (valid) or "error" (invalid) — a generic pass/fail check that is identical for every boundary field (type, asset_types, dimensions, etc.)

**Recommendation**: The assertion is functionally correct for what these boundary tests are designed to verify. The BDD scenario is structured as Given→When→Then where the When step (`_partition_format_ids`) is format_ids-specific — it constructs a `ListCreativeFormatsRequest` with the `format_ids` parameter and calls the real production `_impl`. The Then step checks whether that format_ids-specific call succeeded or failed. The field identity is NOT actually lost — it's encoded in the When step that precedes this Then step. The Then step's job is solely to read the outcome of the already-field-specific When step. Having a generic `_assert_partition_outcome` is correct here because the outcome schema (response vs error) is the same regardless of which field was tested. Renaming to something like `then_format_ids_handling_outcome` would be purely cosmetic — the function already has that name via the step decorator. No assertion change needed.

#### `then_dimension_handling` (tests/bdd/steps/generic/then_payload.py:280)

**Step text**: "the dimension handling should be {expected}"

**Claims**: the function should verify that dimension-specific filtering (by width, height, or both) is handled correctly by the production code

**Actually tests**: checks only whether the operation succeeded (response exists) or failed (error exists) — a binary valid/invalid partition test with no verification that dimension filtering actually filtered by dimensions

**Recommendation**: This is a boundary/partition test, not a behavioral assertion test. The step text "the dimension handling should be valid/invalid" accurately describes a partition-test outcome — it checks whether the system handles the given dimension boundary point without error. For boundary tests, the assertion IS the partition outcome: did the system accept or reject this input class? The step does not claim to verify that returned formats match specific dimension constraints (that would be a separate behavioral scenario like "Then all returned formats should have width >= 300"). The `_assert_partition_outcome` helper is the correct and sufficient assertion for this category of test. The naming pattern is consistent across all sibling boundary steps (type handling, format_ids handling, asset_types handling, responsive handling, name search handling) — all using the same partition-outcome assertion. No change needed.

#### `then_wcag_handling` (tests/bdd/steps/generic/then_payload.py:295)

**Step text**: "the wcag handling should be {expected}"

**Claims**: the step claims to verify that "wcag handling" produces the expected outcome (valid or invalid) for a given boundary input

**Actually tests**: checks whether the production call succeeded (ctx has "response") or errored (ctx has "error") — a generic success/failure check that is identical across all boundary steps

**Recommendation**: the assertion is functionally correct for this scenario's purpose. This is a boundary test: the When step already exercises production code with a specific wcag_level boundary value (first enum, last enum, omitted, unknown string), and the Then step correctly checks whether that boundary input was accepted (valid) or rejected (invalid). The shared helper `_assert_partition_outcome` is the right abstraction here because ALL boundary tests ask the same semantic question: "did the system accept or reject this boundary input?" The wcag-specific validation already happened in the When step (`_partition_wcag`) which called real production code with the wcag_level parameter. No change needed.

#### `then_output_ids_handling` (tests/bdd/steps/generic/then_payload.py:305)

**Step text**: "the output_format_ids handling should be {expected}"

**Claims**: the function should verify that output_format_ids were handled correctly in the partition/filter result

**Actually tests**: delegates to _assert_partition_outcome which checks the generic partition outcome stored in ctx — the same assertion used for input_format_ids handling

**Recommendation**: this is architecturally correct — in BDD, the Given/When steps establish which filter dimension (output_format_ids vs input_format_ids) is under test, and the Then step verifies the resulting partition outcome. The outcome assertion is intentionally identical because the partition result shape is the same regardless of which format_id dimension caused it. The specificity lives in the scenario setup, not the assertion. Having two distinct step text phrases mapping to the same assertion is proper BDD practice: it makes scenarios read naturally ("output_format_ids handling should be excluded" vs "input_format_ids handling should be included") while correctly sharing the underlying verification logic. No change needed.

## All Flagged Steps (Pass 1)

| # | Function | Step Text | Reason |
|---|----------|-----------|--------|
| 1 | `then_error_invalid_params` | the error message should indicate which parameters are inval... | Step claims message indicates *which* parameters are invalid, but assertion only checks `assert msg` (non-empty string) — no check for parameter names or "invalid"/"parameter" keywords. |
| 2 | `then_error_format_id_structure` | the error message should indicate FormatId must include agen... | Step says message should indicate both `agent_url` AND `id` are required, but assertion uses `or` — only one of them needs to appear. Also `formatid` is a loose fallback that doesn't verify the structural requirement. |
| 3 | `then_error_has_fix_suggestion` | the error should include a suggestion for how to fix the iss... | Step says suggestion should indicate *how to fix the issue* (content-specific), but assertion only checks existence and non-emptiness — same as step 9, no content validation that it describes a fix. |
| 4 | `then_suggestion_valid_values` | the suggestion should provide valid parameter values | Step claims "valid parameter values" but only asserts suggestion is non-empty — no content check. |
| 5 | `then_suggestion_agent_url_id` | the suggestion should advise including agent_url (URI) and i... | Step claims suggestion should advise including both "agent_url" AND "id" fields, but assertion only checks for "agent_url" or "uri" — the "id" requirement is never verified. |
| 6 | `then_real_validation_error` | the error should be a real validation error, not simulated | Only checks `"error" in ctx`, not that the error is a real validation error vs simulated |
| 7 | `then_has_referrals` | the response should include creative_agents referrals | Only checks `len(referrals) > 0` (existence), step claims "include creative_agents referrals" implying content validation |
| 8 | `then_format_assets` | each format should include asset requirements with type and ... | Only checks `"type"` in assets, omits `dimensions` which the step text explicitly claims should be validated |
| 9 | `then_type_filtering_result` | the type filtering should result in {expected} | Delegates entirely to `_assert_partition_outcome` — cannot verify without seeing that helper's implementation; the step text "should result in {expected}" implies content validation but the body is opaque |
| 10 | `then_format_ids_filtering_result` | the format_ids filtering should result in {expected} | Same issue as step 9 — delegates to `_assert_partition_outcome` with no visible assertion logic; cannot confirm correctness without the helper |
| 11 | `then_type_handling` | the type handling should be {expected} | `_assert_partition_outcome` receives no field discriminator — cannot verify anything specific about "type handling" vs the other 9 fields; all 10 steps produce identical runtime calls |
| 12 | `then_format_ids_handling` | the format_ids handling should be {expected} | same as #1 — "format_ids handling" check is indistinguishable at runtime from every other field's handler; field identity is lost before assertion |
| 13 | `then_asset_types_handling` | the asset_types handling should be {expected} | same as #1 — "asset_types handling" specific behavior cannot be verified when the helper receives no indication of which field is under test |
| 14 | `then_dimension_handling` | the dimension handling should be {expected} | same as #1 — "dimension handling" claim is not backed by any field-specific assertion path |
| 15 | `then_responsive_handling` | the responsive handling should be {expected} | same as #1 — "responsive handling" is not distinguished from other fields in the shared helper call |
| 16 | `then_name_search_handling` | the name search handling should be {expected} | same as #1 — "name search handling" specific validation is absent; field name is dropped before the assertion |
| 17 | `then_wcag_handling` | the wcag handling should be {expected} | same as #1 — "wcag handling" cannot be individually validated when the helper is called with the same signature as all other fields |
| 18 | `then_disclosure_handling` | the disclosure handling should be {expected} | same as #1 — "disclosure handling" field identity is not passed to the helper; assertion is field-agnostic |
| 19 | `then_output_ids_handling` | the output_format_ids handling should be {expected} | same as #1 — "output_format_ids handling" vs "input_format_ids handling" are indistinguishable at assertion time; both call `_assert_partition_outcome(ctx, expected)` identically |
| 20 | `then_input_ids_handling` | the input_format_ids handling should be {expected} | same as #9 — "input_format_ids handling" and "output_format_ids handling" produce the exact same runtime call; the step text promises distinct validation that is not present |
| 21 | `then_response_status` | the response status should be "{status}" | only checks response existence for "completed"; doesn't verify an actual status field on the response object — the status check is bypassed entirely |
| 22 | `then_no_sandbox_field` | the response should not include a sandbox field | checks sandbox is None but "not include a sandbox field" means the attribute shouldn't exist at all; None could mean present-but-null vs absent |
| 23 | `then_no_real_api_calls` | no real ad platform API calls should have been made | body is just `pass` with a comment rationalizing why — no assertion is made |
