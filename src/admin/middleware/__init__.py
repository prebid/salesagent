"""Admin ASGI middleware package (Flask → FastAPI v2.0 L0 foundation).

Modules in this package are SCAFFOLDED only at L0 — wiring into the canonical
middleware stack (``src/app.py::build_app``) lands at L1a per the
``MIDDLEWARE_STACK_VERSION`` assertion in
``flask-to-fastapi-foundation-modules.md §11.36``.
"""
