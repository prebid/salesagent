# Synthetic violator fixture — used ONLY by the meta-test in
# test_architecture_no_blueprints_dir.py to prove the AST scanner
# actually detects a `from src.admin.blueprints` import.
#
# This file uses a `.txt` extension on purpose so pytest collection
# and the `src.admin.blueprints` import guard itself skip it. The
# meta-test parses it via ast.parse() against the file bytes.
from src.admin.blueprints.auth import auth_bp  # noqa: F401  (synthetic)
